
*test: nestest.expected nestest.actual
  git diff --word-diff=color $ins
  #git diff $ins

nestest.size: nestest.out
  echo $$(cat $< | wc -l) > $out

nestest.golden.simp : simpLog.sh nestest.golden
  ./simpLog.sh nestest.golden > $out

nestest.expected: nestest.golden.simp nestest.size
  cat $in | head -$$(cat nestest.size) > $out

nestest.actual: nestest.out nestest.size # TODO jenga $in1 $In2
  cat nestest.out | head -$$(cat nestest.size) > $out

nestest.out : ../src/emu.exe nestest.nes
  ./$in > $out || true
